53:review. When GET https://api.kotoba.cloud/v1/research/status returns "eligible", the
54:scoped security-research model runs via POST /v1/research/jobs (free daily allowance).
56:(CLI / IDE section, 1 click) and call the same endpoints with
