- 2026-09-08: bench 第194回. 08:10 JST tick. HEAD 79f72cf = bench 第193回 (08:01, K-Z3 8時台帯初計測 run437 済) = remote net-kotobase/main 一致 (git fetch + rev-parse 比較 乖離 0; detached HEAD のため fetch 系で取込, terminal foreground stdout 空=既知のため状態確認・計測出力はファイル書き出し経由)。live smoke 200 (/, /signup; pre-run 計測 — DNS 健在時の liveness)。host load1 52.15 (08:08 uptime 実測, gate 7.5 大幅超過) のため local 測定は拒否。K-Z3 8時台 n 積み増し run439 を試行 したが production search endpoint (search.kotobake.net) の DNS 解決不能 (NXDOMAIN, curl exit 6, HTTP  ​​000 — host/dig 併用で確認; 08:1x 現在 kotobake.net 本体 も解決不能 に移行) により実測不能 — cold/p50/p95 等 digits 一切なし「測れなかった」を真実報告 (host busy (52.15) + DNS 解不能 で測定ゼロ)。status 判定は rank  に委ねる (rank 専門)。secret は一切記録せず。NEXT: 委ねる (rank 指定優先;フォールバックは K-Z3 現在時刻帯 8時台 n 積み増し続行,次 run ID は run440 使用 — run439 枠 は試行のみ で実測なし)。
