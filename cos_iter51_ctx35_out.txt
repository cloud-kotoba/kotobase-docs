search-origin/wrangler.jsonc ['\n  // Prove on workers.dev before attaching search.kotobase.net.\n  // A custom domain belongs to exactly one Worker; the last deploy wins\n  // with no conflict (ADR-2608170600', ' same incident as cypher.kotobase.net).\n  ', 'search.kotobase.net']
ipfs/wrangler.jsonc ['{\n  // net-kotobase-ipfs — the dedicated IPFS/IPNS retrieval Worker for\n  // kotobase.net (see README.md for the full architecture rationale and\n  // ', " serving ipfs.kotobase.net.\n  //\n  // Immutable graph blocks are served directly from the graph database's R2\n  // bucket through KOTOBASE_BLOCKS. B2 is an optional legacy archive tier;\n  // ipfs.io is the final availability fallback. No Kubo daemon or IPFS RPC\n  // endpoint is required by this Worker.\n  ", ' and\n  // re-pointing one of them at a live Worker would resurrect exactly what that\n  // directive retired. kotobase.net is where this belongs; gftd.ai stays dead.\n  ', 'ipfs.kotobase.net', ' and the\n    // other candidate surface -- pinning.kotobase.net -- answers 401 to\n    // GET /ipni/v1/head', ' which no indexer can get past. Serves /ipni/v1/*\n    // plus /health and /_app/meta and 404s everything else; retrieval stays\n    // on ipfs.kotobase.net']
cypher/wrangler.jsonc ['\n  // On until cypher.kotobase.net is attached', ' so the first deploy can be\n  // verified without touching the live hostname. A custom domain belongs to\n  // exactly one Worker and the last deploy wins with no conflict surfaced to\n  // either side — protocols-worker/wrangler.jsonc in net-kotobase records\n  // what that cost when git.kotobase.net moved to an empty store and 28\n  // repositories started answering 404. So: prove the Worker on\n  // *.workers.dev first', '\n  // Attached 2026-08-05. This line is what MOVED cypher.kotobase.net off\n  // kotobase-protocols-worker: a custom domain belongs to exactly one Worker', 'cypher.kotobase.net', 'kotobase.net']
lake/wrangler.jsonc ['\n  // lake.kotobase.net is deliberately NOT claimed here.\n  //\n  // A custom domain belongs to exactly one Worker', " and\n  // neither side is told. net-kotobase's protocols-worker/wrangler.jsonc\n  // records what that cost once already: git.kotobase.net moved onto an empty\n  // store and 28 repositories began answering `404 repository not found`.\n  // net-kotobase/cypher records the same order of operations and follows it.\n  //\n  // So: prove the Worker on *.workers.dev first", 'lake.kotobase.net', 'kotobase.net']
engine/wrangler.jsonc [' kotobase.net cutover\n  // (2026-07-08). Serves ai.gftd.apps.kotobase.datomic.{q', ' below) now serves kotobase.net directly', " under a\n  // kotobase.net-family hostname instead of a separate aozora.app one.\n  //\n  // Existing data note: the previous kotobase.aozora.app deployment's R2\n  // bucket (`kotobase-wasm-staging`) held only empty/test data as of this\n  // cutover (confirmed with the owner) -- no migration was needed.\n  //\n  //   secrets (wrangler secret put): KOTOBA_SEED (production operator\n  //     Ed25519 seed", "\n  // Production has no scheduled trigger while every physical-index rollout\n  // gate below is disabled. Reintroduce the cron atomically with the rollout;\n  // even a no-op scheduled event pays isolate startup and bundle evaluation.\n  // kotobase.net-family Custom Domain (NOT the apex -- that's net-kotobase's\n  // own worker", " which proxies its datomic.* traffic here via\n  // KOTOBA_BACKEND_URL). Cloudflare auto-provisions the proxied DNS record +\n  // edge cert on the same kotobase.net zone net-kotobase's own apex route\n  // already uses (workers:write + zone:read already available there).\n  ", 'backend.kotobase.net']
